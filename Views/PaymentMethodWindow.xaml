<Window x:Class="SimplePOS.Views.PaymentMethodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Seleccionar Método de Pago" Height="400" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#f8fafc"
        FontFamily="Segoe UI">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Método de Pago" FontSize="22" FontWeight="Bold" Margin="0,0,0,20" Foreground="#1e293b" HorizontalAlignment="Center"/>

        <UniformGrid Grid.Row="1" Columns="1">
            <Button x:Name="CashBtn" Click="Method_Click" Tag="Efectivo" Margin="0,5" Height="100" Background="#dcfce7" BorderBrush="#166534" BorderThickness="2">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="💵" FontSize="40" Margin="0,0,20,0"/>
                    <TextBlock Text="EFECTIVO" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Foreground="#166534"/>
                </StackPanel>
            </Button>

            <Button x:Name="MpBtn" Click="Method_Click" Tag="Mercado Pago/Transferencia" Margin="0,5" Height="100" Background="#dbeafe" BorderBrush="#1e40af" BorderThickness="2">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📱" FontSize="40" Margin="0,0,20,0"/>
                    <TextBlock Text="MERCADO PAGO" FontSize="22" FontWeight="Bold" VerticalAlignment="Center" Foreground="#1e40af"/>
                </StackPanel>
            </Button>
        </UniformGrid>

        <Button Grid.Row="2" Content="CANCELAR" Click="Cancel_Click" Height="50" Margin="0,20,0,0" Background="#fee2e2" Foreground="#991b1b" BorderThickness="0" FontWeight="SemiBold"/>
    </Grid>
</Window>
