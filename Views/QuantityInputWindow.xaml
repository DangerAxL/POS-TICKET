<Window x:Class="SimplePOS.Views.QuantityInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ingresar Cantidad" Height="250" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#f8fafc"
        FontFamily="Segoe UI">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="ProductLabel" Text="Producto" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#1e293b"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Cantidad" FontSize="14" Foreground="#64748b" Margin="0,0,0,5"/>
            <TextBox x:Name="QuantityInput" FontSize="32" Padding="10" 
                     Text="1"
                     InputScope="Number"/>
        </StackPanel>

        <Button Grid.Row="2" Content="AGREGAR" 
                Click="Confirm_Click"
                Height="60" FontSize="18" FontWeight="Bold"
                Background="#2563eb" Foreground="White"
                BorderThickness="0">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>
